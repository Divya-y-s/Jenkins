pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'hostname'
            }
        }

        stage('Test') {
            steps {
                sh 'ls -lrt
            }
        }

        stage('Deploy') {
            steps {
                sh 'pwd'
            }
        }
    }
}
