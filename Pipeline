pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'whoami'
            }
        }

        stage('Test') {
            steps {
                sh 'ls -lrt'
            }
        }

        stage('Deploy') {
            steps {
                sh 'pwd'
            }
        }
    }
}
